{
  "schemaVersion": 1,
  "id": "repl",
  "version": "${version}",
  "name": "fabric repl",
  "description": "a repl for fabric server ops to run code on the fly while the server is still on",
  "authors": [
    "sona"
  ],
  "environment": "*",
  "entrypoints": {
    "main": [
      "ur.mom.repl.Repl"
    ]
  },
  "depends": {
    "fabricloader": ">=0.11.3",
    "java": ">=13"
  }
}
